<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
  <link rel="stylesheet" href="styles/layout.css">
</head>
<body>
  <div class="container">
    <h1>My Blog</h1>
    <a id="newPostBtn" href="/new">New Post</a>
    <ul id="postsList">
      <% for(let i = 0; i<posts.length; i++) { %>
        <div class="post">
          <li>
            <h2>
              <%=posts[i].title%>
            </h2>
            <p>
              <%=posts[i].content%>
            </p>
            <a class="edit" href="/<%= posts[i].id %>">Edit</a>
            <a class="delete" href="/delete/<%= posts[i].id %>">Delete</a>
          </li>
        </div>
        
        <% }; %>
    </ul>
  </div>
</body>
</html>