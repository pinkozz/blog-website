<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <link rel="stylesheet" href="styles/layout.css">
<body>
  <div class="container">

    <h1>
      Post
    </h1>

    <% if(locals.post) {%>
      <form id="editForm" method="post" action="/edit/<%=post.id%>">
        <input type="text" name="title" value="<%=post.title%>"><br>
        <textarea type="text" name="content" rows="5" ><%=post.content%></textarea><br>
        <button class="full-width" type="submit">Submit</button>
      </form>
    <%} else {%>
      <form id="newPostForm" method="post" action="/post">
        <input type="text" name="title" placeholder="Title" required><br>
        <textarea type="text" name="content" rows="5" placeholder="Text content" required></textarea><br>
        <button class="full-width" type="submit">Submit</button>
      </form>
    <%}%>

  </div>
</body>
</html>